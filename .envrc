SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

export PATH=$PATH:$SCRIPT_DIR/script

export DB_HOST=localhost
export DB_PORT=5432
export DB_USERNAME=admin
export DB_PASSWORD=
export DB_NAME=ratemyrental
export DB_TEST_NAME=ratemyrental-test
